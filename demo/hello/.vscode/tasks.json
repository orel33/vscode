{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build hello",
            "type": "shell",
            "command": "gcc -std=c99 -Wall hello.c -o hello",
            "group": {
                "kind": "build",
                "isDefault": true
            },"problemMatcher":"$gcc"
        }
    ]
}