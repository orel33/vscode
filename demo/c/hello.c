#include <stdio.h>
#include <math.h>
#include "demo.h"

int main(void)
{
    char name[] = "orel";
    printf("hello world by %s\n", name);
    float x = 100.0f;
    sqrt(x);
    foo(10);
    bar(name);
    return 0;
}
